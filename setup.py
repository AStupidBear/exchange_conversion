from setuptools import setup, find_packages

setup(
    name="exchange_conversion",
    version="0.1.0",
    description="exchange_conversion",
    packages=find_packages(),
    install_requires=[],
)
